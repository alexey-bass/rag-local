# Drop your documents here

Put the files you want to search/ask about in this folder (subfolders are fine too).

Supported formats: `.txt`, `.md` / `.markdown`, `.pdf`

After adding or changing files, rebuild the index:

```
python ingest.py
```
